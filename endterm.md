

# 四則演算のまとめ（Python）

- 加算: `a + b`
- 減算: `a - b`
- 乗算: `a * b`
- 除算（実数）: `a / b`（結果は常に float）
- 余り: `a % b`
- 整数除算: `a // b`
- 累乗: `a ** b`
  例: `5 + 2 = 7`, `5 - 2 = 3`, `5 * 2 = 10`, `5 / 2 = 2.5`
  例: `5 % 2 = 1`, `5 // 2 = 2`, `5 ** 2 = 25`
- 優先順位: `()` → `**` → `* / // %` → `+ -`
- 代入付き演算: `+= -= *= /= //= %= **=`
- 型の注意: `int` と `float` が混ざると結果は `float`

# range の指定方法（Python）

- `range(stop)`：0 から `stop-1` まで
- `range(start, stop)`：`start` から `stop-1` まで
- `range(start, stop, step)`：`start` から `stop-1` まで `step` ずつ進む

# 文字列メソッド isdigit

- 形式: `文字列.isdigit()`
- 意味: 文字列が数字だけで構成されていれば True、それ以外は False

# 文字列のフォーマット（%）

- 形式: `"%フォーマット" % 値`
- 例: `print("%.2f" % num)` は小数点以下 2 桁で表示する

# print の end 引数

- 形式: `print(値, end="文字")`
- 意味: 行末に付ける文字を指定する（改行 `\n` の代わり）
- 例: `print(j, end=" ")` は改行せず空白を付けて続けて表示

# print() のみ

- `print()` は空行（改行だけ）を出力する

# while ループ

- 形式: `while 条件:`
- 意味: 条件が True の間くり返す
- 終了: `break` で強制終了できる
- 例:
  count = 0
  while count < 3:
      print(count)
      count += 1

# len の使い方

- `len(対象)` は関数呼び出しなので丸かっこを使う
- `len[number]` のような書き方はエラー（添字アクセスになってしまう）
- 数字の桁数は `len(str(number))` で求められる
- `len[number]` が使えるのは、`len` をリストや辞書などに代入しているとき（例: `len = [10, 20]`）※組み込み関数を上書きするので非推奨
- 例:
  len = [10, 20, 30]
  print(len[1])  # 20
- 配列（リスト）の長さは `len(リスト)` で調べる
- 例: `numbers = [1, 2, 3]` の長さは `len(numbers)` で 3
- 整数は `number[0]` のように添字アクセスできない（`int` は非対応）
- 数字の特定の桁を取り出すなら `str(number)` に変換してから使う
- 今回の間違い: `int` のまま `number[a-1]` と書いて添字アクセスしようとした

# 関数と return

- `return` は省略できる（その場合は `None` が返る）
- `return` だけ書くと `None` を返して終了する
- 値を返したいときは `return 値` が必要

# バックスラッシュ（行継続）

- `\` は行を続けて書くための記号
- 例:
  total = 1 + \
          2 + 3

# エスケープシーケンス \t

- `\t` はタブ文字（水平タブ）
- 文字列内で列をそろえたいときに使う

# 文字列の置換 replace

- 形式: `文字列.replace(置換前, 置換後)`
- 意味: 文字列内の該当部分を置き換えた新しい文字列を返す
- 例: `name.replace("は", "ひゃ")`

# 部分文字列の検索（スライス）

- `for i in range(len(a) - len(b) + 1):` は b を a の中で探す開始位置を順番に試す
- `a[i:i+len(b)] == b` で一致するか判定する

# スライス

- 形式: `sequence[start:stop]`
- 意味: start から stop-1 までを取り出す
- 例: `text[2:5]` は 2,3,4 番目の文字
- 形式: `sequence[start:]` は start から末尾まで

# for で回す理由（部分文字列検索）

- 開始位置を 0 から順にずらして比較する必要があるため
- ループしないと、どの位置に一致するか判定できない

# float を使う場面

- 小数が必要な計算（例: 割り算の結果を小数で扱う）
- 端数を扱う値（例: 金額、長さ、時間など）
- `int` と `float` が混ざる計算は結果が `float` になる
- `int` では小数を保持できないので、小数を残したいときに使う
- 入力に小数を許可したいときは `float(input(...))` を使う

# 辞書のキーと値

- キー: 変更できない型（ハッシュ可能）が必要（例: `int`, `str`, `tuple`）
- 値: 何でも入れられる（`int`, `str`, `list`, `dict` など）
- キーは重複できず、重複すると後の値で上書きされる

# ファイル書き込みと乱数

- `import random`：乱数モジュールを使う
- `random.randint(1, 100)`：1〜100の整数乱数を1つ生成
- `[... for _ in range(10)]`：リスト内包表記で10個の値を作る
- `with open("tmp.txt", "w") as file`：書き込み用に開き、ブロック終了で自動クローズ
- `file.write(f" {number}\n")`：1行ずつ書き込み（先頭に空白＋改行）
- まとめて書かず、`for` で回して1行ずつ書く

# リスト内包表記

- 形式: `[式 for 変数 in イテラブル]`
- 意味: イテラブルを回して式の結果をリストにする
- 例: `[random.randint(1, 100) for n in range(10)]`
- `(...)` にするとジェネレータ式（リストにならない）なので、`list(...)` か `for` で取り出す

# クラスの基本

- 定義: `class クラス名:`
- 初期化: `def __init__(self, ...):` で属性を設定する
- メソッド: `def メソッド名(self, ...):` でインスタンス操作
- インスタンス生成: `obj = クラス名(引数)`
- 属性アクセス: `obj.属性名`

# 入力と型変換

- `input("...")`：文字列として入力を受け取る
- `int(...)`：文字列を整数に変換する

# f-string

- 形式: `f"{変数}"`
- 意味: 変数の値を文字列内に埋め込む
- `f'...'` でも `f"..."` でもOK（どちらも同じ）
- 例: `name = "taro"` のとき `f"{name}です"` は `taroです`
- `f"{a+b}"` のように式も埋め込める
- `\t` も使える（例: `f"{name}\t{id}"`）

# クラス内の文法（self とメソッド）

- `class Student:`：クラス定義
- `def __init__(self, name, id):`：コンストラクタ（インスタンス生成時に呼ばれる）
- `self`：そのインスタンス自身を指す
- `self.name = name`：インスタンス変数（属性）の代入
- `def change_name(self, new_name):`：インスタンスメソッドの定義

# 辞書の items

- `dict.items()`：キーと値のペアを取り出す
- `for key, value in d.items():`：キーと値を同時に受け取れる
- `for key in d:`：キーだけを回す（値は `d[key]` で取り出す）
- `for value in d.values():`：値だけを回す
- `value += 10` は変数を変えるだけで辞書は更新されないので、`d[key] += 10` が必要
- `for key in d.items():` の `key` は `(キー, 値)` のタプルになるため、`d[key]` は通常エラーになる

# タプル

- 形式: `(a, b, c)` または `a, b, c`
- 意味: 変更できない（イミュータブル）なデータの並び
- リストとの違い: `[]` ではなく `()`、中身を書き換えられない

# 文字列メソッド isdigit

- 形式: `文字列.isdigit()`
- 意味: 文字列が数字だけで構成されていれば True、それ以外は False

# 辞書の初期化とカウント

- `count_dict[num] = 1` はキー `num` が初めて出たときに 1 を入れる（出現回数の初期化）

# ファイルの encoding

- `encoding="utf-8"` は文字コードを UTF-8 に指定する
- 日本語などの文字を正しく読み書きするために使う

# 文字列メソッド strip

- 形式: `文字列.strip()`
- 意味: 文字列の先頭と末尾の空白や改行を取り除く
